import websocket from '../websocket';

describe('SovrenWebSocket', () => {
  it('should instantiate without error', () => {
    expect(websocket).toBeDefined();
  });
  // Add more tests for connect, send, on, off, and reconnect logic as needed
}); 